version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: npm install -g gitbook-cli
      - run: gitbook install
      - run: gitbook build

workflows:
  test:
    jobs:
      - build
